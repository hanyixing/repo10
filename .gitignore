src/main/resources/markdowns/*
!src/main/resources/markdowns/README.md
src/main/resources/dist/
solo_h2_test/
target/
node_modules/
**/.DS_store
.idea/
solo.iml
*.log
/.settings/
/.classpath
/.project

# --- Deployment / environment (sensitive — never commit) ---
# Rendered env files may contain real secrets; only the template is tracked.
.env
.env.*
!.env.example
# Local-only overrides of the per-environment config templates.
*.local.yml
application-*.local.yml
# Runtime artefacts produced by deploy.sh / docker-restart.sh.
logs/
backups/
